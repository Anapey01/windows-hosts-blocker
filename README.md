# windows-hosts-blocker
# Windows Hosts + DNS Content Blocking

This PowerShell script safely blocks adult content sites by modifying the Windows hosts file, applying Cloudflare Family DNS, and locking the hosts file. 

**Important Notes:**
- Run as Administrator
- IPv4 + IPv6 blocked
- Browsers must have DNS-over-HTTPS disabled
- Educational/awareness purposes only
